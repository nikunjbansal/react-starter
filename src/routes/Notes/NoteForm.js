import React from 'react'

export default (props) => {
	return (
		<div>
			{/**<input value={note} onChange={(e) => setNote(e.target.value)} />
		    <input value={initialStatus} readOnly />
		    <button onClick={_handleAdd}>Add</button>*/}
		</div>
	)	
}